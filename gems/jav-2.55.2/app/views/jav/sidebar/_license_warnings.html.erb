<% if license[:id] == 'pro' and license[:valid] == false %>
  <%= render_license_warning title: 'Trial Mode', message: 'You are experiencing paid features that need to be licensed before deploying this site. Enjoy!' %>
<% end %>
<% if license[:error].present? && !(Jav.configuration.display_license_request_timeout_error === false && license[:error] === "Request timeout.") %>
  <%= render_license_warning title: 'Jav HQ Error', message: "#{license[:error]} Checking back every 5 minutes." %>
<% end %>
