class <%= class_name.camelize %> < Jav::Filters::SelectFilter
  self.name = "<%= name.underscore.humanize %>"
  # self.visible = -> do
  #   true
  # end

  def apply(request, query, value)
    query
  end

  def options
    {}
  end
end
